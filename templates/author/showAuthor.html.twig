<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Détails de l’auteur</title>
</head>
<body>
    <h1>Détails de l’auteur</h1>

    <p><strong>Nom :</strong> {{ author.username|upper }}</p>
    <p><strong>Email :</strong> {{ author.email }}</p>
    <p><strong>Nombre de livres :</strong> {{ author.nb_books }}</p>
    <img src="{{ asset(author.picture) }}" alt="{{ author.username }}" width="200">

    <br><br>
    <a href="{{ path('listAuthor') }}">⬅ Retour à la liste</a>
</body>
</html>
