{% extends 'base.html.twig' %}

{% block title %}Détail du service{% endblock %}

{% block body %}
    <div style="text-align: center; margin-top: 50px;">
        <h1>Service : {{ name }}</h1>

        <p>Bienvenue sur la page du service <strong>{{ name }}</strong>.</p>

        <div style="margin-top: 20px;">
          
            <a href="{{ path('app_home') }}">🏠 Aller à l'accueil (HomeController)</a>
        </div>
    </div>
{% endblock %}
